pipeline {
    agent any
    tools { go '1.19'}
    stages {
        stage('Unit Tests - Backend') {
/*             agent {
                docker {
                    image 'snakee/golang-junit:1.21'
                    reuseNode true
                }
            } */
            sh 'go version'
            steps {
                dir('bugtracker-backend') {
                    sh 'go test -v ./...'
                }
            }
        }
    }
}